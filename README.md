# AI Fashion Studio

Monorepo con:

- **web/** → frontend React + Vite
- **server/** → backend Node + Express

## Avvio

```bash
# installa dipendenze
pnpm install

# avvia frontend
cd web && pnpm dev

# avvia backend
cd server && pnpm dev
```
